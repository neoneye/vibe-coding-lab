# Response to `COMMIT_REVIEW_69d3f99.md` — 2026-09-07

Every finding was reproduced with the reviewer's own `review_probes_69d3f99.py` before anything was changed (all six reproduce), then verified against the code, then repaired. The repaired checker is the one in the tree at the commit that adds this file; the negative controls the review asked for run in the suite (`dev/sweep_proof_arb_controls.py`, 19 checks, via `check_arb.js`).

| # | Finding | Verified? | Repair | Where |
|---|---|---|---|---|
| 1 | Intervals exclude the exact value: float hulls in `pl_range`, `pl_slope_range`, the bilinear helpers and the kernel `_hull`; float acceptance comparison | **Yes**, all four probes | Every hull is now built from the balls' exact endpoints (`kernel_pieces_arb.hull`/`ball`; `arb.lower()` is an exact arb); slopes computed in balls; breakpoints enter as their certified balls, not float midpoints; the verdict comparisons are `val.lower() >= arb(target)` between exact balls. Controls: exact-rational interpolant, slope, bilinear value and the direct kernel evaluation are *contained* in the point ranges; the `2^-70`-below-target value is refused. | `sweep_proof_arb.py`, `kernel_pieces_arb.py`, controls 1–5 |
| 1b | Breakpoint completeness not established ("finite sign scan") | **Yes** — the scan proved nothing about missed roots | `kernel_pieces_arb.coverage`: the gaps between consecutive certified balls are proved zero-free by interval bisection, the piece touching a ball by strict monotonicity across the hull of ball and piece (its derivative excludes zero there, so the ball's one certified zero is the only one). A `Pieces` object refuses to construct if either proof fails; `kernel_pieces_arb.py` reports both. `w''` got a sinc-form implementation so the proof works across the closed form's removable singularity. | `kernel_pieces_arb.py` (checks 6–7 of 12) |
| 1c | *Found while repairing 1:* the kernel tables stopped at distance 30 whatever the tape's cube; beyond 30 a "range" was the hull of its two endpoint values with no critical points — not a range. The cube-16 `sharp` tape has pair distances up to 96, the cube-28 pair tape up to 168, and `w` decays only like `1/x²` (its maxima near 96 are `7.6e-6`), so a far box could have been enclosed optimistically by up to `~4e-5`. | Found by the new limit check refusing the full tape | The table limit follows the tape: `6·cube + 1`, coverage proved to that limit (1.7 s for 169). | `sweep_proof_arb.Cert(limit)` |
| 2 | A one-byte `LEAF_TUBE` tape on the no-tube candidate, target 1000, exits 0 | **Yes** | Tube leaves are refused outright (the emitter never writes one). The tube *exclusion* — whole root pieces dropped by the partition — is now bound to its theorem: the candidate must be the stationary pair candidate, `tube_arb.results.json` must have been produced from the candidate and certificate files in use (hash equality) and passed, and every excluded piece must lie, compared exactly in Arb, inside a certified tube of its phase. For the sharp candidate any positive `tubeRadius` is refused. Opcodes with a coordinate index ≥ 6 are refused. Controls 7, 8, 17. | `sweep_proof_arb.py` (partition returns the excluded pieces), controls |
| 2b | *Found while binding the tube:* `tube_arb.py` certified phase 0 only (`LOW, HIGH, …`), while the sweep excludes both alternating phases; the pair candidate is **not** reversal-symmetric (1188 bilinear entries differ from their mirror), so phase 1 was excluded with no theorem. And the sweep excludes the tube as a *float* box whose cuts `fl(c ± 0.008)` stick out of the exact radius-0.008 tube by `6.9e-18`. | Yes (numerically at the phase-1 centre `R − E_alt = +2.2e-15`, gradient `1e-15`) | `tube_arb.py` certifies both phases at radius `0.00800000000000001`, which covers the float box (checked exactly), λ ≥ 0.104984 (phase 0) and 0.107108 (phase 1); the checker records the floor inside the tubes, `E_alt − shortfall`, separately from the tape's target. | `tube_arb.py` (14 checks), `sweep_proof_arb.results.json` → `tube` |
| 3 | Sources hashed at completion; shards executed pre-edit code under post-edit hashes | **Yes** — shard 0 started 05:36, the bilinear rewrite landed 05:57, all eight shards carry its hash | Hashes are taken at start, before any work, carried in every checkpoint, re-hashed at the end; a difference is a failed check. The results carry the full option set and the exact replay command. **All eight overnight shards are demoted to diagnostics** (0 refuted, 13 unresolved of 48M — under code that is now known to have had faults 1, 1c); the complete verification is re-run under the frozen checker (see the status table in `REPORT.md`). | `sweep_proof_arb.py`, control 19 |
| 4 | Out-of-shard samples counted into shard totals (217/219 extras on shard 0) | **Yes** | In `--all` mode nothing is sampled; the results carry the shard's own obligation counts and a check that the verdicts account for exactly those; unresolved obligations are identified by root index and tape offset. The aggregator (`sweep_proof_aggregate.py`, now in `dev/`) requires: all-nodes mode, shard-complete status, same tape/candidate/inputs across shards **and equal to the files on disk now**, no source change during any run, nothing skipped or unresolved, root ranges that partition `[0, roots)` exactly, and obligations summing to the tape's leaves and collapses. | `sweep_proof_arb.py`, `sweep_proof_aggregate.py` |
| 5 | Full verification exits 0 with unresolved obligations | **Yes** | `--all` adds three checks — nothing skipped by `--limit`, verdicts account for the shard, nothing unresolved — and a `status` field: `complete`, `shard complete (roots a:b of n)`, `incomplete`, or `sample`. Controls 9–10. | `sweep_proof_arb.py` |
| 6 | `Δ ≥ D1` is false: ten reals `1.06 + j/10000` plus `±i/20` give `Δ − D1 = −0.01306…` | **Yes**, reproduced in float64 and certified in Arb inside the script | D1 marked **refuted** in `LAMZOURI_STABILITY.md`; the counterexample and every other one are written with full coordinates to `lamzouri_slack.counterexamples.json`; stage 5 described as a numerical experiment. | `lamzouri_slack.py`, `LAMZOURI_STABILITY.md`, `REPORT.md` |
| doc | `REPORT.md` contradicts itself on the transcript's status; superseded counts | Yes | `REPORT.md` rewritten around one status table with a dated log beneath it. | `REPORT.md` |

## What the review did not ask for but changed as a consequence

- `check_arb.js` gained the controls unit; every transcript the checker writes was regenerated with the repaired checker (`sweep_proof_arb.results.json`, `sweep_proof_sharp_small.results.json`, the full-tape samples). The old refine-4 sample-220 transcript of the full sharp tape (`sweep_proof_sharp_full.results.json`) was removed as superseded by the 5000-sample one.
- The reviewer's probe script is kept as is; run it against the repaired code and the first three lines read `False`, the forged proof exits 1, the unresolved control exits 1, and the D1 line is unchanged (it is a fact about the mathematics, not the code).

## What remains open, stated plainly

- The complete verification of the `sharp` tape is **done** — not by this Python checker (the owner stopped its eight-hour shard run) but by a compiled sibling, `dev/tapecheck`, with the same semantics and checks: every obligation confirmed, `tapecheck_sharp_full.results.json`. The Python checker remains the reference the compiled one is compared against (small domain in full, full tape on a 5000+5000 sample).
- Even complete, the tape is a check of a *double-precision* sweep's claims in Arb; the Lean theorem `sharp_chain_bound_signed_concrete` still takes `hcob` as a hypothesis, and no mechanism imports the checker's verdict into Lean.
- The pair tape's floor inside its tubes is `E_alt − 2.19e-15`, not the target; the tape proves the target on the complement only. The number is in the transcript.
