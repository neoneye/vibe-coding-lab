# Claim ledger

Stage 1 of `CLAUDE_INVESTIGATION_PLAN.md`. Baseline: commit `107fbd9` (2026-09-07), macOS 24.6, Node v22.17.0, Python 3.14.6 (`mpmath` 1.4.1; `python-flint` 0.9.0 in a scratch venv, used through `ARB_PYTHON`), Lean via elan (`~/.elan/bin`). Suite: `sh dev/run_suite.sh` from `riemann-stab/` (with `ARB_PYTHON` set) — green end to end on 2026-09-08 after the 14 legacy rows without current input hashes were regenerated (they had made it exit 1 at `tiling_interval_test.js` since before this investigation, reproduced at commit `540aa38`). Fast gate: `node dev/test.js` (93), `labs_shipcheck.js`, `tiling_pressure_test.js`, `build.js`, `ui_dom_smoke.js`. Arb: `ARB_PYTHON=<venv python> node dev/check_arb.js` re-ran every Arb program on 2026-09-07; all transcripts reproduced.

Strength scale: **search** (float optimiser; an upper bound on an infimum) < **subdivision** (double precision, exhaustive) < **enclosure** (proved bounds, this page's arithmetic) < **Arb** (independent ball arithmetic) < **Lean** (kernel-checked; hypotheses listed) < **theorem about ζ** (all inputs discharged).

| claim | statement | strength | artifact | reproduce | outstanding |
|---|---|---|---|---|---|
| H = 0.6725007037 | ≥ 67.25% of zeros simple on the line | theorem about ζ (Lean, arXiv:2608.13637; reproof 2609.02882) | external | — | human review "ongoing" per the page's chip |
| Lamzouri Prop. 2.1 on this page | (2.4), (2.5) hold on 1015 multisets; kernel matches an 80-digit oracle to 1.4e−15 relative | high-precision numerical comparison (80-digit mpmath, no proved enclosure) of a Lean-certified statement | `investigation/labf_oracle.py` | `node dev/investigation/labf_dump.js && python dev/investigation/labf_oracle.py` | none; see `LAB_F_AUDIT.md` |
| live bound at T = 600 | (2N−S)/N = 0.92320 on 341 zeros | exact finite statement on computed zeros | Lab F·ii | run the page | zeros assumed on the line (Lab B's argument-principle count) |
| C_MT three ways | closed form, Q₀-form, R(ψ_MT) agree (5.5e−12; 1.2e−7 for the midpoint rule) | double-precision numerics against a closed form | self-test rows 9–10 | `node dev/test.js` | — |
| K² = tiling weight | Lamzouri's kernel squared equals `overlapWeight` to 1e−13 | numerics | `test.js` | same | — |
| isolated block minimum 0.003826231211 | `F6 ≥` this at a specific block | search (upper bound) | `tiling_research.golden.json` | `tiling_research_test.js` | — |
| `F6 ≥ 19/5000` | Ainta's per-block certificate | subdivision (control, 3 147 403 boxes), Arb externally | `tiling_interval.results.json` | `node dev/sweep.js fast bare 0.0038` | not Lean |
| chain floor 0.003956 (`sharp`) | telescoping certificate ≥ 0.003956 on `[0,16]⁶`, tail lemma beyond | enclosure (67 608 431 boxes, re-run 2026-09-07, checksum `37308f214a50bb25`, current hashes) | `tiling_interval.results.json` `rigorousRuns` | `node dev/sweep.js rigorous sharp 0.003956` (79 min) | not replayed by the suite (too large); its double-precision proof tape (74.7M nodes) is structurally verified in full and Arb-checked on a 5000+5000 sample with 0 refuted under the checker repaired after `COMMIT_REVIEW_69d3f99` — a sample, not a complete arithmetic verification; **complete** independent check of every obligation of the tape by the compiled checker `dev/tapecheck` (48 006 226 obligations, all confirmed, 41 min, `tapecheck_sharp_full.results.json`); the overnight Python shards of 2026-09-07 ran unfrozen code and are diagnostics only; still not Lean, and `hcob` is still a hypothesis of the Lean theorem |
| chain floor = E_alt − 2.2e−15 (pinned pair) | alternating chain is the minimiser | enclosure outside the tubes (75 004 893 boxes), **Arb** inside the tubes | `tiling_pair.sweep.rigorous.json`, `tube_arb.results.json` | `check_pair_sweep.js`; `tube_arb.py` | sweep half is modulo the page's arithmetic; proof tape (cube 1.6, the small configuration) 220/220 leaves and 220/220 collapses confirmed in Arb; the cube-28 tape (66.1M nodes) structurally verified with a 5000+5000 sample; the tube exclusion is now bound to `tube_arb.py`, which certifies **both** alternating phases at radius 0.00800000000000001 (the float box the sweep excludes sticks 6.9e−18 out of the exact 0.008 tube; the pair candidate is not reversal-symmetric, so phase 1 needed its own certificate — it had none until 2026-09-07); inside the tubes the floor is E_alt − 2.19e−15, not the target; no complete independent arithmetic check of the exterior sweep yet |
| Lamzouri slack correction `Δ ≥ D1` | simple-simple pair energy bounds the slack | **refuted** (external review, 2026-09-07): ten reals `1.06 + j/10000` plus `±i/20`, `Δ − D1 = −0.01306…`, certified in Arb | `investigation/lamzouri_slack.counterexamples.json` | `python3 dev/investigation/lamzouri_slack.py` | stage 5 is a numerical experiment; no valid universal correction found |
| local coercivity, walls, branches, resonances, staircase | as stated in the note | **Arb** | `*_arb.results.json` | `check_arb.js` | wall infimum uncertified (upper bounds unconditional) |
| Lean claims (16 theorems) | telescoping, cyclic identity, projection pins, MT complement | Lean, standard axioms | `dev/lean/ZetaClaims.lean` | `run_suite.sh` (Lean step) | none of them is about ζ |
| projection 0.6731102697 | alternating energy through `projectedSimpleZeroBound` | arithmetic on a conditional formula | `tiling_research.golden.json` | `tiling_research_test.js` | **hypothesis mismatch**: formula is `n_point_bound`'s, whose hypothesis is per-block; the window-sum variant is now Lean (`n_point_bound_of_windowSum`); the endpoint loss is the state potential's oscillation `B`, not the tail amplitude — conservative score 0.6731051 (sharp), 0.6730895 (pair); signed-cap score 0.6731094 (sharp, W = 252) / 0.6731098 (pair, W = 250) under `n_point_bound_signed`, now Lean. See `ASSEMBLY_AUDIT.md` |
| n = 8 peak, p* = 3370.45 | block-size and pressure optima | search | note | `tiling_pressure_test.js` | conditional on the same lemma; `n` is a free parameter of the Lean theorem, `p` is a **natural number** there, so 3370.45 needs integer neighbours |
| zeta-lab Φ₃ = 0.6727373, Φ₄ = 0.6728470 | unconditional for Mathlib's `riemannZeta` | Lean, registry-replayed, **rebuilt here** (8905 jobs, exit 0; `#print axioms` = `[propext, Classical.choice, Quot.sound]` for all seven declarations), unreviewed | external clone at `47d0241` | `cd lean/bridge && lake exe cache get && lake build V2Challenge V2Solution` | human review of statement faithfulness |

Superseded statements found and fixed on 2026-09-07 (third pass, after `COMMIT_REVIEW_69d3f99`): "`D1` is a valid correction term" → refuted by an adversarial cluster; "the overnight shards verify the sharp tape" → they ran unfrozen code, diagnostics only; the tape checker's ranges were not enclosures (float hulls, tables stopping at 30, unproved breakpoint completeness) → repaired and controlled. Second pass added: "the amplitude bounds the endpoint loss" → the endpoint oscillation `B` does (sharp 0.00171, not 0.00128); "39/220 belongs to the 75M sweep" → it belongs to the cube-1.6 pair tape; "the reviewer's 20-digit K(0.4i) fails past digit 16" → it was the exact-decimal input, correct to 20 digits. Earlier: "Arb confirms 64 … 59" (note) → 39 / 67 per the transcript; "‖f_z‖² = K(0) = 1 for all z" (page, spec) → `K(z − z̄)`; "not checkable here at all" (note, page) → identified missing lemma; "0.6731102697 is the whole of the available improvement" → boundary term must be paid.
